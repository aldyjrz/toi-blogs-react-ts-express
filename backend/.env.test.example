# Backend integration test environment
# Copy to backend/.env.test and fill DATABASE_URL with a dedicated TEST database
# (never reuse the production database).
# The integration suite reads DATABASE_URL (or DATABASE_URL_TEST) and skips
# gracefully when it is not reachable.

DATABASE_URL="postgresql://postgres:[YOUR-TEST-PASSWORD]@db.rphcxmmdadqqqgogfusp.supabase.co:5432/postgres?schema=public"
JWT_SECRET="test-jwt-secret"
JWT_REFRESH_SECRET="test-refresh-secret"
